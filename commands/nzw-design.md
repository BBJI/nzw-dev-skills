---
description: nzw UI/UX 设计规范 — 根据需求文档产出设计令牌、组件规格、高保真稿
---

请触发 `design-skill` 根据 `.nds/01-requirements/` 中的需求文档生成 UI/UX 设计规范。

执行要求：
1. 读取 `.nds/state.json`，将 `current_phase` 设为 `design`
2. 产出设计令牌 JSON、组件规格、用户流程图、高保真 HTML 稿到 `.nds/02-design/`
3. 同步刷新 `.nds/PROGRESS.md`
